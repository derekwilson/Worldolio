<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:utility="clr-namespace:WorldolioMauiPOC.Utility"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="WorldolioMauiPOC.Views.Partial.FontIconButton"
             x:Name="thisPartial"
             >
    <Border 
        Grid.Row="2" 
        StrokeShape="RoundRectangle 8"
        Padding="0"
        WidthRequest="40"
        HeightRequest="40"
        HorizontalOptions="Center"
        VerticalOptions="Center">

        <Border.Behaviors>
            <toolkit:TouchBehavior
                DefaultBackgroundColor="{StaticResource White}"
                PressedBackgroundColor="{StaticResource PrimaryLight}"
                HoveredBackgroundColor="{StaticResource Gray100}"
                Command="{Binding Source={x:Reference thisPartial}, Path= ClickCommand}"
            />
        </Border.Behaviors>

        <Label 
            Text="{Binding IconGlyph, Source={x:Reference thisPartial}}" 
            FontFamily="{Binding FontName, Source={x:Reference thisPartial}}" 
            FontSize="30" 
            TextColor="{AppThemeBinding Light={StaticResource OffBlack},
                                            Dark={StaticResource White}}"
            VerticalTextAlignment="Center" 
            HorizontalTextAlignment="Center" 
        />

    </Border>
</ContentView>
