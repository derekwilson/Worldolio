<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="WorldolioMauiPOC.Views.CityGrid"
             Title="Worldolio">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior EventName="Loaded" Command="{Binding InitCommand}" />
    </ContentPage.Behaviors>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Settings"
                     IconImageSource="settings.png"
                     Order="Primary"
                     Priority="1"
                     Clicked="Toolbar_Settings"
                     >
        </ToolbarItem>
        <ToolbarItem Text="About"
                     IconImageSource="info.png"
                     Order="Primary"
                     Priority="2"
                     Command="{Binding NavigateToAboutPage}" 
                     >
        </ToolbarItem>
    </ContentPage.ToolbarItems>
    
    <Grid RowDefinitions="auto,*">
        <Label
            Padding="5"  
            >
            <Label.FormattedText>
                <FormattedString>
                    <Span Text="Time: " TextColor="Gray" />
                    <Span Text="{Binding CurrentTime}" FontAttributes="Bold" />
                </FormattedString>
            </Label.FormattedText>
        </Label>
        <CollectionView 
                x:Name="citiesCollection"
                Grid.Row="1"
                ItemsSource="{Binding Cities}"
                Margin="5"
                SelectionMode="Single"
                SelectionChanged="citiesCollection_SelectionChanged"
                >
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame 
                        Style="{StaticResource Frame.DataTemplate}"
                        >
                        <Grid   
                            Padding="5"  
                            ColumnDefinitions="Auto,*,Auto"  
                            RowDefinitions="Auto, Auto, Auto, Auto">

                            <Label  
                                Style="{StaticResource Label.BigGridItem}"
                                HorizontalTextAlignment="Center"
                                Grid.RowSpan="2"
                                Text="{Binding CurrentDay}" />

                            <Label  
                                Style="{StaticResource Label.BigGridItem}"
                                Grid.Row="2"
                                Grid.RowSpan="2"
                                Text="{Binding CurrentTime}" />

                            <HorizontalStackLayout
                                Grid.Column="1"
                                >
                                <Label  
                                    Style="{StaticResource Label.LittleGridItem}"
                                    FontAttributes="Bold"  
                                    Text="{Binding CityName}" />
                                <Label  
                                    Style="{StaticResource Label.LittleGridItem}"
                                    Text="{Binding CountryName}" />
                            </HorizontalStackLayout>

                            <HorizontalStackLayout
                                Grid.Column="1"
                                Grid.Row="1"
                                >
                                <Label  
                                    Style="{StaticResource Label.LittleGridItem}"
                                    Text="{Binding OffsetToHome}" />
                                <Label  
                                    Style="{StaticResource Label.LittleGridItem}"
                                    Text="{Binding DSTDates}" />
                            </HorizontalStackLayout>

                            <HorizontalStackLayout
                                Grid.Column="1"
                                Grid.Row="2"
                                >
                                <Label  
                                    Style="{StaticResource Label.LittleGridItem}"
                                    Text="Sun Rise" />
                                <Label  
                                    Style="{StaticResource Label.LittleGridItem}"
                                    Text="{Binding Sunrise}" />
                                <Label  
                                    Style="{StaticResource Label.LittleGridItem}"
                                    Text=", Set" />
                                <Label  
                                    Style="{StaticResource Label.LittleGridItem}"
                                    Text="{Binding Sunset}" />
                            </HorizontalStackLayout>

                            <HorizontalStackLayout
                                Grid.Column="1"
                                Grid.Row="3"
                                >
                                <Label  
                                    Style="{StaticResource Label.LittleGridItem}"
                                    Text="Moon Rise" />
                                <Label  
                                    Style="{StaticResource Label.LittleGridItem}"
                                    Text="{Binding Moonrise}" />
                                <Label  
                                    Style="{StaticResource Label.LittleGridItem}"
                                    Text=", Set" />
                                <Label  
                                    Style="{StaticResource Label.LittleGridItem}"
                                    Text="{Binding Moonset}" />
                            </HorizontalStackLayout>
                            
                            <!--
                            <Button
                                Grid.Column="2"
                                Grid.RowSpan="4"
                                x:Name="DetailsBtn"
                                Text="Details" 
                                VerticalOptions="Center"
                                SemanticProperties.Hint="Details for the city"
                                Clicked="DetailsBtn_Clicked"
                                />
                            -->

                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>